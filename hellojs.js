document.addEventListener('DOMContentLoaded', getWeather);

document.getElementById('someRandomID').addEventListener('mousedown', abotherJSFunc);


function getWeather() {
  console.log('this is a test msg');
  console.log('this is a test msg');
  console.log('this is a test msg');
}

function abotherJSFunc() {

  switch (key) {
    case value:
      
      break;
  
    default:
      break;
  }
  for (let idx = 0; idx < array.length; idx++) {
    if (true) {
      if (condition) {
        
      }
    } else {
      
    }
    const elem = array[idx];
    console.log("current element index is " + elem);
  }
  console.log("exceeded my expectations");
  
}